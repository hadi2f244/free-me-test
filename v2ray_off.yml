
- hosts: all
  gather_facts: True
  tasks:
    - name: v2ray disconnect
      include_role:
        name: "hadi2f244.free_me.v2ray"
      vars:
        connected: false
        set_env_proxy: true
        set_apt_proxy: true
        set_container_proxy: true
      when: use_freeme == true
